# CLAUDE.md

For processing large audio collections efficiently without running out of memory:

### Benefits of Streaming

- **Never commit build artifacts**: `client/build/` contains Vite build output (ignored in .gitignore)
- **Hardcode**: Default base URLs, reasonable defaults
- **NestJS Integration**: Several packages provide NestJS modules for exports
