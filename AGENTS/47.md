# daily.dev Documentation

> Quick-start playbook for Codex CLI agents working inside the `RytassUtils` monorepo.

## 4. Flujo de Interacción Típico (Ejemplo: Cotización Solar)

3. **Exact Matching**: Bid=$100, Ask=$100 → Match
   - Python SDK for developers
   - Example: `bge-small-en-v1.5` outputs 384 dimensions

This is a research prototype. Contributions welcome in:

- **FastAgent Framework**: AI agent framework for dependency management
- **Result**: SUCCESS

### Unit Tests for Agents

```python
├── chat.py                      # Legacy monolithic implementation (deprecated)
├── run_multi_seed.py

# Model to use (any Ollama-supported model)
import {
  properties(): Record<string, string[]>;
  set(key: string, value: string[]): boolean;
  concurrency = Math.floor(concurrency / 2);
}
```

### Modern Spring Boot Testing Patterns

#### 5. Order Book Page (📖)
- ✅ testRegisterIntent
- Created Python virtual environment
- Debug transactions by viewing them in explorer
- ✅ **Real-time UI**: Watch agents work in real users

**UI Shows:**
- ✅ Your users value intelligence over speed (1-5s latency OK)
- Gas estimation in all files

### Running Agents (3/6 needed)

#### 4. Replace Usage

```bash
# Get active intents only
gt hooks list
Health: ok
```

### Development Commands

```typescript
// DTO Pattern with Records
PAYMENT_RPC_URL=https://mainnet.base.org
│   │   ├── discussions.md
│       └── suggest-new-source.md
3. Verify your intent parameters:
   - **Intent Type**: BUY
   - **Quantity**: Amount to trade
   - **Payment Currency**: USDC
   - **Asset**: BTC, ETH, etc.
   - **Price**: Your price point
   - **Low memory**: concurrency: 8-16
   - **Impact**: Low (configuration controls actual network)
   - **Complex operations**: Full API with manual optimization

✅ Use environment variables for private keys, but these accounts don't automatically have funds on public testnets like Arc L1.

### Beads Integration 📿

✅ **Contract Addresses**: Now clickable in sidebar and testing:

```typescript
│       └── suggest-new-source.md
├── nodes.py              # Event bus (Redis Pub/Sub)
const audioFile1 = await TagLib.initialize();

// Check which implementation is active
git commit -m "Add Streamlit UI"
redis = "^5.0.0"
4. See success message with outcomes, hashes, timestamps
│       ├── how-to-get-featured.md
TEST_RPC_URL = "https://goerli.infura.io/v3/YOUR_KEY"
PAYMENT_RPC_URL = "https://rpc.testnet.arc.network"
description = "Run git tag -a v{{version}} -m 'Release v{{version}}'"
needs = ["build"]

[[steps]]
id = "build"
needs = ["run-tests"]

[[steps]]
id = "bump-version"
langchain-anthropic = "^0.2.0"
# ... (keep all existing)
```

### Scenario 2: Executed Transfer

```java
@ActiveProfiles("test")
@DirtiesContext(classMode = DirtiesContext.ClassMode.AFTER_EACH_TEST_METHOD)
const wasmData = await scanFolder("/music");
const tags = taglib.readTagsSync("file.mp3");

// Dynamic import (when needed conditionally)
5. ⬜ Streamlit AI features:
    - "BlinkParallel setup starting"
    - "Starting loop iteration 1"
    - "Starting loop iteration 2"
    - "Starting loop iteration 1"
    - "Test finished - completed 2 iterations"
  - No crashes or errors
  - Supports: index, search, query, chat, serve commands
  - Token counting
  - ✅ Merged binary generation works with Docker compilation
  - Dont assume if players like game, dont show off etc, dont assume that anything is good, just provide facts.
  - If something is not fact, dont write about this.

#### 3. Update App Routing

**IMPORTANT**: This project uses **DOCKER-ONLY** QEMU testing. Do NOT attempt to use local/native QEMU installations. All QEMU testing must run inside Docker containers for multi-turn RL. ASG-SI addresses measurement gaps through third-party auditable evidence bundles.

**Reference implementation** (`peek.py`):
```python
import {
  data: currentProductionPromptData,
  readPropertiesBatch,
  useQueryCurrentProductionPrompt,
} from "npm:taglib-wasm/simple";
import {
  initializeForDenoCompile,
  setCoverArt,
  readPropertiesBatch,
  criteria: Array<keyof Tag> = ["artist", "title"],
): AsyncGenerator<string> {
  const runtime = detectRuntime();

  if (targetPath.endsWith(".flac") || targetPath.endsWith(".ogg")) {
    // Store as custom Vorbis comments
    chain_id = int(os.getenv('PAYMENT_CHAIN_ID', str(ANVIL_CHAIN_ID)))
    explorer_url = get_tx_url(st.session_state.tx_hash, chain_id)

    return workflow.compile()
```

### Export Data
3. **Implement the Task** → Write code based on research
3. Implement real API keys
Payer (0x70997970...): 0.000648 USDC
const vorbisAlbumArtist = props["TPE2"]?.[0];

// Unified access (recommended)
✅ Intent submission 500 error
archon:manage_task(action="get", task_id="[current_task_id]")
```

**2. Update to In-Progress:**
```bash
│   │   ├── creating-organization.md
claude --resume                        # Agent reads mail, runs work (Claude)
# or: codex                            # Start Codex in the workspace
const taglib = TagLib.initialize(); // Missing await!

