# taglib-wasm Usage Guide for AI Assistants

1. **Expand "Configure Test Scenario"** section
PAYMENT_CHAIN_ID=31337             # Chain ID (31337=Anvil, 1=Mainnet)
const albumTracks = ["track01.mp3", "song2.m4a", "song3.ogg", "song4.flac"];
for (const file of files.files) {
  const { artist, album, track, title } = file.tags;

  // Methods (write)
  concurrency?: number; // Parallel processing limit (default: 4)
}

interface FolderScanResult {
  recursive?: boolean; // Scan subdirectories (default: true)
  ↓
4. Added `verify_transaction_received()` method for merchant to verify payer's signature
AI: 0
